# Required on Vercel for persistent global leaderboard storage.
# Apache-2.0 dependency @vercel/blob reads this token server-side only.
BLOB_READ_WRITE_TOKEN=
# Required to view /stats.html and GET /api/stats.
# Use a long random value. Do not commit real admin keys.
STATS_ADMIN_KEY=
